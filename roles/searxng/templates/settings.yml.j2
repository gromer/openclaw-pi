use_default_settings: true
server:
  secret_key: "{{ openclaw_secrets.searxng_secret_key | default('VALIDATION_ONLY_NOT_FOR_PRODUCTION') }}"
  bind_address: "0.0.0.0"
  port: 8080
  limiter: false
  image_proxy: false
search:
  safe_search: 1
  formats: [html, json]
