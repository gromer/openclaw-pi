# Production inventory

This directory is ignored. Copy `inventories/example` here, replace all example
values, and create `group_vars/secrets.sops.yml` using `make secrets-init`.
Never force-add plaintext inventory or an age identity.

